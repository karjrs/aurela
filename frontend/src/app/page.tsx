import { UsersList } from "@/components/users/UsersList";

const Home = () => {
  return (
    <div className="bg-day-arc flex flex-1 flex-col">
      <UsersList />
    </div>
  );
};

export default Home;
